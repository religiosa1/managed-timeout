export const callback = "Expeting to get a cb function as the first argument" as const;
export const delay = "Delay argument should be a number >= 0" as const;
export const repeatedStart = "Timeout was already started" as const;
export const abort = "The timeout was cancelled" as const;